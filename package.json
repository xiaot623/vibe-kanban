{
  "name": "vibe-kanban",
  "version": "0.1.22",
  "private": true,
  "scripts": {
    "frontend:lint": "cd frontend && npm run lint",
    "backend:lint": "cargo clippy --workspace --all-targets --all-features -- -D warnings",
    "frontend:format": "cd frontend && npm run format",
    "backend:format": "cargo fmt --all",
    "frontend:check": "cd frontend && npm run check",
    "backend:check": "cargo check",
    "frontend:dev:raw": "cd frontend && PORT=$(node scripts/setup-dev-environment.js frontend) npm run dev -- --port ${PORT:-} --host",
    "frontend:dev": "bash ./scripts/run-pm2-dev.sh vibe-frontend-dev frontend:dev:raw",
    "backend:dev:raw": "BACKEND_PORT=$(node scripts/setup-dev-environment.js backend) DISABLE_WORKTREE_ORPHAN_CLEANUP=1 RUST_LOG=debug cargo watch -w crates -x 'run --bin server'",
    "backend:dev": "bash ./scripts/run-pm2-dev.sh vibe-backend-dev backend:dev:raw",
    "mobile:dev": "cd frontend/mobile-ui && export PORT=$(node ../../scripts/setup-dev-environment.js mobile-ui) && pnpm install && pnpm run dev --port ${PORT:-} --host 0.0.0.0",
    "lint": "npm run frontend:lint && npm run backend:lint",
    "format": "npm run frontend:format && npm run backend:format",
    "check": "npm run frontend:check && npm run backend:check",
    "dev": "concurrently \"npm run backend:dev\" \"npm run frontend:dev\"",
    "frontend:build": "cd frontend && npm run build",
    "mobile:build": "cd frontend/mobile-ui && pnpm run build",
    "test:npm": "./test-npm-package.sh",
    "dev:qa": "export FRONTEND_PORT=$(node scripts/setup-dev-environment.js frontend) && export BACKEND_PORT=$(node scripts/setup-dev-environment.js backend) && concurrently \"npm run backend:dev:watch:qa\" \"npm run frontend:dev\"",
    "generate-types": "cargo run --bin generate_types",
    "generate-types:check": "cargo run --bin generate_types -- --check",
    "prepare-db": "node scripts/prepare-db.js",
    "prepare-db:check": "node scripts/prepare-db.js --check",
    "version:show": "node scripts/sync-version.js show",
    "version:set": "node scripts/sync-version.js set",
    "version:bump": "node scripts/sync-version.js bump",
    "build:npx": "bash ./local-build.sh",
    "prepack": "npm run build:npx",
    "release": "pnpm run check && pnpm run desktop:build && git add -A && git commit -m \"chore: release v$npm_package_version\" && git push origin release:release && git tag -a \"v$npm_package_version\" -m \"Release v$npm_package_version\" && git push origin \"v$npm_package_version\"",
    "pre_release": "node scripts/pre-release.js",
    "desktop:dev:raw": "npm run frontend:build && cd crates/desktop && pnpm exec tauri dev",
    "desktop:dev": "bash ./scripts/run-pm2-dev.sh vibe-desktop-dev desktop:dev:raw",
    "desktop:build": "npm run frontend:build && cd crates/desktop && pnpm exec tauri build",
    "desktop:build:debug": "npm run frontend:build && cd crates/desktop && pnpm exec tauri build --debug",
    "android:dev": "npm run mobile:build && cd crates/desktop && pnpm exec tauri android dev",
    "android:build": "npm run mobile:build && cd crates/desktop && pnpm exec tauri android build --target aarch64",
    "android:build:debug": "npm run mobile:build && cd crates/desktop && pnpm exec tauri android build --target aarch64 --debug"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.0",
    "concurrently": "^8.2.2",
    "vite": "^6.3.5"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "packageManager": "pnpm@10.13.1",
  "dependencies": {
    "react-virtuoso": "^4.18.1"
  }
}
